extends index
block MainView
  fieldset(align="center")
    legend 새 항목은 #{title} 아래에 만들어 집니다.
    form(action="/category/newcategory" method="post" style="display:inline-block; margin:15px;")
      p
        label Title
      p
        input(type="text" name="title")
      p
        input(type="submit" value="Create")
