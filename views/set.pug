extends index
block MainView
  ul
    if children.length == 0
      li child이 없습니다. 첫 child을 만들어 주세요!
    each child in children
      li
        a(href="/subset/"+child._id+"/"+child.title)
          h2= child.title
