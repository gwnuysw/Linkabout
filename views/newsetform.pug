extends header
block MainView
  fieldset(align="center")
    legend 새 항목은 #{cursettitle} 아래에 만들어 집니다.
    form(action="/set/newset/"+cursetid+"/"+cursettitle method="post" style="display:inline-block; margin:15px;")

      label(for="title") Title -
        input(type="text" name="title" id="title")
      input(type="submit" value="Create")
